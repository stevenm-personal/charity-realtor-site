{
  "name": "charity-realtor-site",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test:contact": "node --test --test-isolation=none tests/contact-worker.test.mjs tests/contact-client.test.mjs",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.3.0",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.120.0"
  }
}
